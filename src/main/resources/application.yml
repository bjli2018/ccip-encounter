spring:
  application:
    name: ccip-encounter
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ccip_encounter?serverTimezone=GMT%2B8
    username: root
    password: admin
server: 
  port: 8091

eureka: 
  client:
    service-url: 
      defaultZone: http://localhost:8761/eureka

mybatis:
  mapperLocations: classpath:mapping/*.xml
  configuration:
    mapUnderscoreToCamelCase: true